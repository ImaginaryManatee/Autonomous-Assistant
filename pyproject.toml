[project]
name = "autonomous-assistant"
version = "0.1.0"
description = "Local-first, private background context engine for AAC hardware."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ollama>=0.6.2",
    "faiss-cpu>=1.14.3",
    "numpy>=2.0.0",
    "websockets>=16.0",
]
